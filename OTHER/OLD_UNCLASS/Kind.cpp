#include <iostream>
#include <cstdio>
#include <algorithm>
#include <algorithm.
#include <vector>

using namespace std;

bool kind(vector<int> arr, int d);

int main()
{

}
bool kind(vector<int> arr, int d)
{
    sort(arr.begin, arr.end());
}
